# Copy to .env and fill in. docker-compose reads this automatically.

# Strong random secret that protects all /admin endpoints. REQUIRED.
ADMIN_TOKEN=change-me-to-a-long-random-string

# PostgreSQL credentials (used by both the db and api containers).
PGUSER=dotweaver
PGPASSWORD=change-me-too
PGDATABASE=dotweaver
